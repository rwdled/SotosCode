import string
import time
import os
text = "The first fully AI layerd two block chain for - by - LLM (Lotta little money) Technogly "

temp = ''
for ch in text:
    for i in string.printable:
      if i == ch or ch == '':
        time.sleep(0.03)
        print(temp + i)
        temp +=ch
        break
    else:
       time.sleep(0.02)
       print(temp+i)

os.system('clear')
text2 = "proof of - entry scan system, broke - detection software Theromdynamic software one goraillen dps   "

temp2 = ''
for ch in text2:
    for i in string.printable:
      if i == ch or ch == '':
        time.sleep(0.03)
        print(temp + i)
        temp +=ch
        break
    else:
       time.sleep(0.02)
       print(temp+i)
